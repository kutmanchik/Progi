#include <bits/stdc++.h>
using namespace std;

struct Student
  {
    string id;
    int score;

  };
    int main()
    {
      int n,idd,sco;
      cin>>n;
      Student a[n];
      while(n+1 >= 0)
      {
        for (int i=0; i<n; i++)
        {
          cin>>a[idd].id;
          cin>>a[sco].score;
        }
        n--;
      }

      return 0;
    }
